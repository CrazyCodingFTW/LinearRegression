language: csharp
script:
  - xbuild /p:Configuration=LinearRegression.sln
before_install:
  - sudo apt-get install nunit-console
before_script:
  - nuget restore solution-name.sln
after_script:
  - nunit-console LinearRegression.Tests/bin/Debug/LinearRegression.Tests.dll